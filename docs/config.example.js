// Supabase 접속 정보 (공개 anon key — RLS로 읽기만 허용되므로 노출돼도 안전함)
// 이 파일을 docs/config.js 로 복사한 뒤 아래 값을 실제 값으로 채우고 커밋하세요.
// (service_role 키는 절대 여기에 넣지 마세요 — 그건 .env 전용입니다.)

export const SUPABASE_URL = 'https://xxxxxxxxxxxx.supabase.co';
export const SUPABASE_ANON_KEY = 'your-anon-key';
